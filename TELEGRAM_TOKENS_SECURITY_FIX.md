# 🚨 КРИТИЧЕСКИЙ ИНЦИДЕНТ БЕЗОПАСНОСТИ - Telegram Bot Tokens

## Обнаружена утечка Telegram Bot Tokens

**Дата обнаружения:** 2024-12-XX  
**Статус:** Требуется немедленное действие

## 🔴 Обнаруженные скомпрометированные токены

### Токен 1 (Alert Bot):

- **Токен:** `8354072320:AAFAjqaI_dshIJeeLGG8fAhwO2pqllRbu0c`
- **Назначение:** Telegram Alert Bot (уведомления об ошибках)
- **Обнаружен в:** `docs/TELEGRAM_ALERTS_SETUP.md`
- **Статус:** ❌ Скомпрометирован

### Токен 2 (Main Bot):

- **Токен:** `8232314768:AAEabExGz6iDfe2wTrDSB600Qu97kyl1ta4`
- **Назначение:** Основной Telegram Bot (DEV и PROD)
- **Обнаружен в:** `docs/ENVIRONMENTS.md`, `docs/GITHUB_DEPLOY_SETUP.md`
- **Статус:** ❌ Скомпрометирован

## ⚡ НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### 1. Отозвать скомпрометированные токены

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/mybots`
3. Выберите соответствующий бот:
   - Для Alert Bot: найдите бота, который использовал токен `8354072320:...`
   - Для Main Bot: найдите бота, который использовал токен `8232314768:...`
4. Выберите **API Token** → **Revoke current token**
5. Подтвердите отзыв токена

**⚠️ ВАЖНО:** После отзыва токена бот перестанет работать до создания нового токена!

### 2. Создать новые токены

Для каждого бота:

1. В @BotFather выберите бота → **API Token** → **Generate new token**
2. Скопируйте новый токен (он показывается только один раз!)
3. Сохраните токен в безопасном месте (не в репозитории!)

### 3. Обновить токены в GitHub Secrets

1. Перейдите в GitHub → Settings → Secrets and variables → Actions
2. Обновите следующие секреты:

#### Для Alert Bot:

- `TELEGRAM_ALERT_BOT_TOKEN` → новый токен

#### Для Main Bot:

- `TELEGRAM_BOT_TOKEN_DEV` → новый токен для development
- `TELEGRAM_BOT_TOKEN_PROD` → новый токен для production (если используется отдельный бот)

### 4. Обновить токены в .env файлах

**⚠️ ВАЖНО:** Убедитесь, что `.env` файлы в `.gitignore` и не коммитятся в репозиторий!

Обновите токены в локальных `.env` файлах:

```bash
# Alert Bot
TELEGRAM_ALERT_BOT_TOKEN=<новый_токен>

# Main Bot
TELEGRAM_BOT_TOKEN_DEV=<новый_токен_dev>
TELEGRAM_BOT_TOKEN_PROD=<новый_токен_prod>
```

### 5. Проверить активность ботов

В течение следующих 7-30 дней:

1. Проверяйте логи ботов на подозрительную активность
2. Мониторьте сообщения, отправленные ботами
3. Проверяйте, не были ли созданы новые команды или webhooks

### 6. Очистить историю Git (рекомендуется)

Секреты все еще находятся в истории Git. Для полного удаления:

#### Вариант 1: Использовать BFG Repo-Cleaner

```bash
# Создайте файл tokens.txt с токенами для замены:
# 8354072320:AAFAjqaI_dshIJeeLGG8fAhwO2pqllRbu0c==>REMOVED
# 8232314768:AAEabExGz6iDfe2wTrDSB600Qu97kyl1ta4==>REMOVED

bfg --replace-text tokens.txt

# Очистите рефлоги
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# Принудительно обновите удаленный репозиторий
git push --force
```

#### Вариант 2: Использовать git filter-branch

```bash
# Удалите токены из всей истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch docs/TELEGRAM_ALERTS_SETUP.md docs/ENVIRONMENTS.md docs/GITHUB_DEPLOY_SETUP.md" \
  --prune-empty --tag-name-filter cat -- --all

# Принудительно обновите удаленный репозиторий
git push origin --force --all
git push origin --force --tags
```

## 📋 Чеклист восстановления

- [ ] Отозваны все скомпрометированные токены в @BotFather
- [ ] Созданы новые токены для всех ботов
- [ ] Обновлены все секреты в GitHub Secrets
- [ ] Обновлены токены в локальных .env файлах
- [ ] Удалены токены из всех файлов в репозитории
- [ ] Обновлена документация (без реальных токенов)
- [ ] Проверены все другие места хранения токенов
- [ ] Настроен мониторинг активности ботов
- [ ] Команда проинформирована об инциденте

## 🛡️ Предотвращение будущих утечек

### Правила работы с токенами:

1. **НИКОГДА не коммитьте токены в Git**
   - Используйте `.env.local` для локальной разработки
   - Добавьте `.env*` в `.gitignore`
   - Используйте GitHub Secrets для CI/CD

2. **Используйте плейсхолдеры в документации:**

   ```bash
   # Правильно
   TELEGRAM_BOT_TOKEN=<ваш_bot_token>

   # Неправильно
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz  # НИКОГДА!
   ```

3. **Регулярно ротируйте токены:**
   - Меняйте токены каждые 90 дней
   - Используйте разные токены для dev/prod окружений
   - Отзывайте токены при подозрении на компрометацию

4. **Используйте инструменты для проверки:**
   - `git-secrets` - предотвращает коммиты с секретами
   - `truffleHog` - сканирует историю Git на наличие секретов
   - GitHub Secret Scanning (уже включен)

5. **Проверяйте документацию:**
   - Используйте плейсхолдеры: `<ваш_bot_token>`
   - Не включайте реальные токены в примеры
   - Регулярно проверяйте документацию на наличие секретов

## 📝 История инцидента

- **2024-12-XX:** Обнаружена утечка токенов через GitHub Secret Scanning
- **2024-12-XX:** Токены удалены из файлов репозитория
- **2024-12-XX:** [Дата отзыва токенов]
- **2024-12-XX:** [Дата создания новых токенов]
- **2024-12-XX:** [Дата обновления GitHub Secrets]

## 📞 Контакты для экстренных ситуаций

- **Telegram BotFather:** @BotFather
- **GitHub Security:** https://github.com/security
- **Telegram Support:** https://telegram.org/support

---

**ВАЖНО:** Этот документ содержит конфиденциальную информацию. Не коммитьте его в публичный репозиторий или удалите после устранения инцидента.
