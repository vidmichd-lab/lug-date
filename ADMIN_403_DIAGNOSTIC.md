# Диагностика ошибки 403 в админке

## Быстрая проверка в браузере

### 1. Откройте консоль браузера (F12) и выполните:

```javascript
// Проверить токен
const token = localStorage.getItem('admin_token');
console.log('Token:', token);
console.log('Token length:', token?.length);

// Проверить API URL
const apiUrl = window.ADMIN_CONFIG?.API_URL || import.meta.env.VITE_API_URL;
console.log('API URL:', apiUrl);

// Проверить текущий origin
console.log('Current origin:', window.location.origin);
```

### 2. Проверьте запросы в Network tab:

1. Откройте DevTools → Network
2. Попробуйте загрузить события
3. Найдите запрос к `/api/admin/management/events`
4. Проверьте:
   - **Request Headers** → `Authorization: Bearer <token>` - должен быть
   - **Request Headers** → `Origin: <ваш-url-админки>` - должен быть
   - **Response** → Status code и body

### 3. Проверьте логи в консоли:

Должны быть логи:

- `🔑 Adding auth token to request: /events Token: ...`
- Если нет - токен не передается

## Возможные проблемы

### Проблема 1: Токен не сохраняется при логине

**Решение:**

1. Очистите localStorage: `localStorage.clear()`
2. Перезагрузите страницу
3. Залогиньтесь снова
4. Проверьте: `localStorage.getItem('admin_token')`

### Проблема 2: Токен не передается в запросах

**Проверка:**

- В консоли должны быть логи `🔑 Adding auth token to request`
- В Network tab должен быть заголовок `Authorization: Bearer ...`

**Решение:**

- Проверьте, что токен есть в localStorage
- Проверьте, что interceptor работает (см. `admin/src/api/management.ts:28-48`)

### Проблема 3: CORS блокирует запрос

**Проверка:**

- В Network tab проверьте Response Headers
- Если нет `Access-Control-Allow-Origin` - проблема CORS

**Решение:**

- Проверьте `ADMIN_ORIGINS` в GitHub Secrets
- Убедитесь, что URL админки точно совпадает (включая https://)

### Проблема 4: Токен не совпадает с ADMIN_TOKEN на бекенде

**Проверка:**

- Получите токен при логине: `localStorage.getItem('admin_token')`
- Проверьте `ADMIN_TOKEN` в GitHub Secrets
- Они должны совпадать

**Решение:**

- Если не совпадают - обновите `ADMIN_TOKEN` в GitHub Secrets
- Передеплойте бекенд
- Залогиньтесь заново

## Проверка бекенда

### 1. Проверьте логи бекенда в Yandex Cloud Console

Ищите записи:

- `admin_auth_check` - показывает проверку авторизации
- `admin_auth_failed` - показывает причину ошибки
- `cors_check` или `cors_blocked` - показывает проверку CORS

### 2. Проверьте переменные окружения контейнера

Убедитесь, что установлены:

- `ADMIN_ORIGINS` - должен содержать URL админки
- `ADMIN_TOKEN` - должен совпадать с токеном при логине

## Быстрое решение

Если ничего не помогает:

1. Очистите localStorage:

```javascript
localStorage.clear();
location.reload();
```

2. Залогиньтесь снова

3. Проверьте токен:

```javascript
console.log('Token:', localStorage.getItem('admin_token'));
```

4. Проверьте запросы в Network tab

5. Если все еще 403 - проверьте логи бекенда
