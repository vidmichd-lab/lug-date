# Админ-панель: Дашборд аналитики

Дашборд с ключевыми метриками для мониторинга приложения.

## 📊 Метрики

### Общая статистика

- **Пользователи**: всего, новых за неделю, рост
- **События**: активные, прошедшие, всего
- **Матчи**: всего, за день/неделю, рост
- **Конверсия**: лайки → матчи, просмотры → лайки, просмотры → матчи
- **Онлайн**: количество активных пользователей сейчас

### Графики

1. **Регистрации пользователей** (линейный график)
   - Регистрации по дням
   - Активные пользователи

2. **Популярность событий** (барчарт)
   - Топ-10 событий по лайкам
   - Показывает лайки, просмотры, матчи

3. **Воронка конверсии** (funnel chart)
   - Просмотры → Лайки → Матчи
   - Процент конверсии на каждом этапе

4. **Активность по дням недели** (heatmap)
   - Активность по часам и дням
   - Визуализация пиков активности

### Real-time метрики

- **Онлайн пользователей**: обновляется каждые 30 секунд
- **Последние матчи**: лента с анимацией появления новых матчей

## 🔄 Автообновление

Все данные обновляются автоматически каждые 30 секунд с помощью React Query.

## 📥 Экспорт отчетов

### PDF

Экспорт включает:
- Общую статистику
- График регистраций
- Топ событий
- Воронку конверсии

### Excel

Экспорт в формате .xlsx с несколькими листами:
- Общая статистика
- Регистрации
- Топ событий
- Воронка

## 🚀 Использование

```bash
# Запуск админки
npm run dev:admin

# Дашборд доступен по адресу
http://localhost:5174
```

## 📡 API Endpoints

Все endpoints находятся в `/api/admin/analytics/`:

- `GET /overview` - Общая статистика
- `GET /users-chart?period=7d` - График пользователей
- `GET /events-top?limit=10` - Топ событий
- `GET /funnel` - Воронка конверсии
- `GET /activity-heatmap` - Активность по дням
- `GET /recent-matches?limit=10` - Последние матчи

## 🎨 Компоненты

- `OverviewStats` - Карточки со статистикой
- `UsersChart` - График регистраций
- `EventsTopChart` - Топ событий
- `FunnelChart` - Воронка конверсии
- `ActivityHeatmap` - Heatmap активности
- `RecentMatches` - Последние матчи
- `ExportButtons` - Кнопки экспорта



