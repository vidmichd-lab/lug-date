# Настройка ключей доступа к Yandex Object Storage

Инструкция по добавлению ключей для автоматической загрузки frontend.

## 🔑 Ваши ключи

**Имя бакета:** `telegram-app-frontend`

**Access Key ID:** `YCAJE...

**Secret Access Key:** `YCPG...` (получите в Yandex Cloud Console)

---

## ⚠️ Важно: Безопасность

**НИКОГДА не коммитьте эти ключи в Git!**

- ✅ Файл `.env` уже в `.gitignore` - это правильно
- ❌ Не добавляйте ключи в код
- ❌ Не публикуйте ключи в открытых репозиториях
- ✅ Используйте GitHub Secrets для CI/CD

---

## Шаг 1: Добавление ключей в .env

1. **Откройте файл `.env`** в корне проекта
   - Если файла нет, создайте его

2. **Добавьте следующие строки:**

```bash
# Yandex Object Storage для frontend деплоя
YANDEX_STORAGE_BUCKET_DEV=telegram-app-frontend
YANDEX_STORAGE_ACCESS_KEY_DEV=YCAJE...
YANDEX_STORAGE_SECRET_KEY_DEV=YCPG... (ваш секретный ключ)
OBJECT_STORAGE_URL=https://storage.yandexcloud.net
```

3. **Сохраните файл**

---

## Шаг 2: Проверка настройки

Проверьте, что ключи правильно настроены:

```bash
# Проверка переменных окружения
npm run deploy:frontend
```

Если все настроено правильно, вы увидите:
- ✅ Сборка frontend
- ✅ Загрузка файлов в Object Storage
- ✅ URL вашего приложения

---

## Шаг 3: Использование

### Локальная загрузка:

```bash
# Собрать и загрузить frontend
npm run deploy:frontend

# С полной очисткой бакета перед загрузкой
npm run deploy:frontend:clear
```

### Автоматическая загрузка через GitHub Actions:

Для автоматической загрузки при push в репозиторий нужно добавить ключи в GitHub Secrets:

1. Перейдите в GitHub → Settings → Secrets and variables → Actions
2. Добавьте следующие secrets:

**Для Environment: staging (ветка develop):**
- `YANDEX_STORAGE_BUCKET_DEV` = `telegram-app-frontend`
- `YANDEX_STORAGE_ACCESS_KEY_DEV` = `YCAJE...
- `YANDEX_STORAGE_SECRET_KEY_DEV` = `YCPG...` (ваш секретный ключ)

**Для Environment: production (ветка main):**
- Можно использовать те же ключи или создать отдельные для production

---

## Что дальше?

После настройки ключей:

1. **Попробуйте загрузить frontend:**
   ```bash
   npm run deploy:frontend
   ```

2. **Проверьте URL:**
   - Скрипт покажет URL вашего приложения
   - Обычно: `https://telegram-app-frontend.website.yandexcloud.net/`

3. **Используйте URL в BotFather:**
   - Откройте `@BotFather` в Telegram
   - Отправьте `/setmenubutton`
   - Вставьте URL вашего приложения

---

## Частые проблемы

### Проблема: "Object Storage credentials not found"

**Решение:**
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что переменные добавлены правильно
- Убедитесь, что нет лишних пробелов в значениях

### Проблема: "Access Denied"

**Решение:**
- Проверьте, что ключи скопированы полностью (без пробелов)
- Убедитесь, что сервисный аккаунт имеет права на запись в бакет
- Проверьте, что имя бакета указано правильно

### Проблема: "Bucket not found"

**Решение:**
- Убедитесь, что бакет `telegram-app-frontend` существует
- Проверьте правильность написания имени бакета
- Убедитесь, что используете правильный регион

---

## Полезные ссылки

- [Yandex Object Storage Documentation](https://cloud.yandex.ru/docs/storage/)
- [Инструкция по деплою](YANDEX_OBJECT_STORAGE_DEPLOY.md)
- [Настройка GitHub Actions](GITHUB_DEPLOY_SETUP.md)

---

**Последнее обновление:** 2024-12-01

