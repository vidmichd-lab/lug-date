# Деплой Frontend в Yandex Object Storage (Без терминала)

Полная инструкция по загрузке Telegram Web App в Yandex Cloud через веб-интерфейс.

## 🎯 Преимущества

- ✅ Не требует терминала
- ✅ Только российские сервисы (Yandex Cloud)
- ✅ Стабильный HTTPS URL
- ✅ Бесплатный тариф
- ✅ Работает в России

---

## Шаг 1: Подготовка файлов

### ⭐ Вариант 1.1: Автоматическая сборка и загрузка (Рекомендуется!)

**Самый простой способ - одна команда:**

```bash
npm run deploy:frontend
```

Эта команда автоматически:
1. Соберет frontend
2. Загрузит все файлы в Object Storage
3. Покажет URL вашего приложения

**Требования:**
- Настроены переменные окружения в `.env`:
  - `YANDEX_STORAGE_BUCKET` (или `YANDEX_STORAGE_BUCKET_DEV`)
  - `YANDEX_STORAGE_ACCESS_KEY` (или `YANDEX_STORAGE_ACCESS_KEY_DEV`)
  - `YANDEX_STORAGE_SECRET_KEY` (или `YANDEX_STORAGE_SECRET_KEY_DEV`)

### Вариант 1.2: Сборка через GitHub Actions (Автоматически)

Если у вас настроен GitHub Actions, файлы соберутся и загрузутся автоматически при push в репозиторий.

1. Сделайте commit и push:
```bash
git add .
git commit -m "Update frontend"
git push
```

2. Дождитесь завершения деплоя в GitHub Actions
3. Готово! Файлы автоматически загружены в Object Storage

### Вариант 1.3: Сборка вручную (если есть доступ к терминалу)

Если у вас есть доступ к терминалу или можете попросить коллегу:

```bash
# Перейдите в папку проекта
cd /path/to/lug

# Установите зависимости (если еще не установлены)
npm install

# Соберите frontend
npm run build:frontend
```

После сборки файлы будут в папке `frontend/dist/`

### Вариант 1.4: Использование готовых файлов

Если у вас уже есть собранные файлы, переходите к Шагу 2.

---

## Шаг 2: Создание бакета в Yandex Object Storage

1. **Откройте Yandex Cloud Console:**
   - Перейдите на https://console.cloud.yandex.ru/
   - Войдите в свой аккаунт

2. **Перейдите в Object Storage:**
   - В меню слева найдите **"Object Storage"**
   - Нажмите на него
   - Выберите **"Buckets"** (Бакеты)

3. **Создайте новый бакет:**
   - Нажмите синюю кнопку **"Создать bucket"** (вверху справа)
   - Заполните форму:
     - **Имя бакета:** `telegram-app-frontend` (или любое уникальное имя)
       - ⚠️ Имя должно быть уникальным глобально (только латинские буквы, цифры, дефисы)
     - **Тип доступа:** Выберите **"Публичный"** (Public)
     - **Регион:** Выберите ближайший (например, `ru-central1`)
   - Нажмите **"Создать bucket"**

---

## Шаг 3: Загрузка файлов

1. **Откройте созданный бакет:**
   - Найдите ваш бакет в списке
   - Нажмите на его имя

2. **Загрузите файлы:**
   - Нажмите кнопку **"Загрузить объекты"** (вверху)
   - В открывшемся окне:
     - Нажмите **"Выбрать файлы"**
     - Выберите **ВСЕ файлы** из папки `frontend/dist/`
     - ⚠️ Важно: Загрузите все файлы, включая:
       - `index.html`
       - Все файлы из папки `assets/` (JS, CSS, изображения)
       - Все остальные файлы
   - Нажмите **"Загрузить"**

3. **Проверьте загрузку:**
   - Убедитесь, что все файлы загружены
   - Особенно важно наличие `index.html` в корне бакета

---

## Шаг 4: Настройка веб-сайта

1. **Откройте настройки бакета:**
   - В бакете нажмите на вкладку **"Настройки"** (Settings)
   - Прокрутите до раздела **"Веб-сайт"** (Website)

2. **Включите веб-сайт:**
   - Включите переключатель **"Веб-сайт"**
   - В поле **"Индексный документ"** введите: `index.html`
   - В поле **"Документ ошибки"** (опционально) введите: `index.html`
   - Нажмите **"Сохранить"**

---

## Шаг 5: Получение URL

После настройки вы получите URL вашего приложения:

### Вариант 5.1: Прямой URL Object Storage

URL будет выглядеть так:
```
https://storage.yandexcloud.net/telegram-app-frontend/index.html
```

Или без `/index.html` (если настроен индексный документ):
```
https://storage.yandexcloud.net/telegram-app-frontend/
```

### Вариант 5.2: Через CDN (рекомендуется)

Для лучшей производительности настройте CDN:

1. **Создайте CDN ресурс:**
   - В меню Yandex Cloud выберите **"CDN"**
   - Нажмите **"Создать ресурс"**
   - Заполните:
     - **Источник:** Выберите ваш бакет Object Storage
     - **Доменное имя:** Автоматически или укажите свое
   - Нажмите **"Создать"**

2. **Получите CDN URL:**
   - После создания CDN ресурса вы получите URL вида:
   ```
   https://your-cdn-id.cdn.yandexcloud.net/
   ```

---

## Шаг 6: Настройка в BotFather

1. **Откройте Telegram:**
   - Найдите бота `@BotFather`
   - Откройте чат с ним

2. **Настройте кнопку меню:**
   - Отправьте команду `/setmenubutton`
   - Выберите вашего бота из списка
   - Введите текст кнопки: `Открыть приложение` (или любой другой)
   - Введите URL вашего приложения:
     - Если используете Object Storage: `https://storage.yandexcloud.net/telegram-app-frontend/`
     - Если используете CDN: `https://your-cdn-id.cdn.yandexcloud.net/`
   - Нажмите **"Отправить"**

3. **Готово!**
   - Кнопка меню настроена
   - Теперь при нажатии на кнопку в боте откроется ваше приложение

---

## Шаг 7: Проверка работы

1. **Откройте вашего бота в Telegram**
2. **Нажмите кнопку меню** (внизу чата)
3. **Приложение должно открыться** внутри Telegram
4. **Проверьте функциональность:**
   - Приложение должно загрузиться
   - Должны работать все страницы
   - API запросы должны работать (если backend настроен)

---

## Обновление приложения

### Способ 1: Автоматическая загрузка (Рекомендуется) ⭐

**Самый простой способ - одна команда:**

```bash
npm run deploy:frontend
```

Эта команда:
1. Соберет frontend (`npm run build:frontend`)
2. Автоматически загрузит все файлы из `frontend/dist/` в Object Storage
3. Покажет URL вашего приложения

**Для полной очистки перед загрузкой:**
```bash
npm run deploy:frontend:clear
```

### Способ 2: Через GitHub Actions (Автоматически)

Если настроен GitHub Actions, при каждом push в ветки `main` или `develop`:
1. Frontend автоматически соберется
2. Файлы автоматически загрузятся в Object Storage
3. Никаких действий не требуется!

**Настройка GitHub Actions:**
- См. раздел "Автоматизация через GitHub Actions" ниже

### Способ 3: Ручная загрузка

Если нужно загрузить вручную:

1. **Соберите новую версию:**
   ```bash
   npm run build:frontend
   ```

2. **Загрузите новые файлы в Object Storage:**
   - Откройте бакет в Yandex Cloud Console
   - Удалите старые файлы (или загрузите новые поверх)
   - Загрузите новые файлы из `frontend/dist/`

3. **Очистите кэш CDN (если используете):**
   - В CDN ресурсе нажмите **"Очистить кэш"**
   - Или подождите автоматической очистки (обычно несколько минут)

---

## Частые проблемы

### Проблема: "404 Not Found"

**Решение:**
- Убедитесь, что `index.html` загружен в корень бакета
- Проверьте настройку "Индексный документ" в настройках бакета
- Убедитесь, что тип доступа бакета - "Публичный"

### Проблема: "Файлы не загружаются"

**Решение:**
- Проверьте, что все файлы из `frontend/dist/` загружены
- Убедитесь, что структура папок сохранена (особенно `assets/`)
- Проверьте права доступа к бакету

### Проблема: "Приложение не открывается в Telegram"

**Решение:**
- Убедитесь, что URL использует HTTPS (не HTTP)
- Проверьте, что URL доступен в браузере
- Убедитесь, что в BotFather указан правильный URL

### Проблема: "Старая версия показывается"

**Решение:**
- Очистите кэш CDN (если используете)
- Подождите несколько минут для обновления кэша
- Проверьте, что загружены новые файлы

---

## Автоматизация через GitHub Actions

Для автоматической сборки и загрузки настроен GitHub Actions workflow.

**Файл:** `.github/workflows/deploy-frontend.yml`

**Как работает:**
- При push в ветку `main` → деплой в production бакет
- При push в ветку `develop` → деплой в staging бакет
- Автоматически собирает frontend
- Автоматически загружает файлы в Object Storage

**Настройка Secrets в GitHub:**

📖 **Подробная инструкция с вашими ключами:** [GITHUB_STORAGE_SECRETS.md](GITHUB_STORAGE_SECRETS.md)

**Краткая инструкция:**

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте следующие secrets:

**Для Staging (ветка develop):**
- `FRONTEND_STORAGE_BUCKET_DEV` = `telegram-app-frontend`
- `FRONTEND_STORAGE_ACCESS_KEY_DEV` = `YCAJE...
- `FRONTEND_STORAGE_SECRET_KEY_DEV` = `YCPG...

**Для Production (ветка main):**
- `FRONTEND_STORAGE_BUCKET_PROD` = `telegram-app-frontend` (или отдельный)
- `FRONTEND_STORAGE_ACCESS_KEY_PROD` = `YCAJE...
- `FRONTEND_STORAGE_SECRET_KEY_PROD` = `YCPG...

**⚠️ Важно:** Используем префикс `FRONTEND_` чтобы не конфликтовать с существующими `YANDEX_STORAGE_*` secrets для backend.

**Пример workflow (уже создан):**

```yaml
name: Deploy Frontend to Yandex Object Storage

on:
  push:
    branches: [main, develop]
    paths:
      - 'frontend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build frontend
        run: npm run build:frontend
      
      - name: Configure Yandex CLI
        run: |
          curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
          echo "${{ secrets.YC_SERVICE_ACCOUNT_KEY }}" > yc-key.json
          yc config profile create github || true
          yc config set service-account-key yc-key.json
          yc config set cloud-id ${{ secrets.YC_CLOUD_ID }}
          yc config set folder-id ${{ secrets.YC_FOLDER_ID }}
      
      - name: Upload to Object Storage
        run: |
          yc storage cp -r frontend/dist/* s3://telegram-app-frontend/
```

---

## Полезные ссылки

- [Yandex Object Storage Documentation](https://cloud.yandex.ru/docs/storage/)
- [Yandex CDN Documentation](https://cloud.yandex.ru/docs/cdn/)
- [Telegram Web App Documentation](https://core.telegram.org/bots/webapps)

---

**Последнее обновление:** 2024-12-01

