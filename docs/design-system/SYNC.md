# Синхронизация дизайн-системы с Figma

Автоматическая синхронизация дизайн-системы из Figma позволяет обновлять компоненты и токены без ручного программирования.

## 🚀 Использование

### Ручная синхронизация

```bash
npm run sync:figma
```

Скрипт:
1. ✅ Проверяет изменения в Figma файле (по дате обновления)
2. ✅ Скачивает обновленные компоненты
3. ✅ Перезаписывает только измененные файлы
4. ✅ Создает `CHANGELOG.md` с списком изменений
5. ✅ Выводит предупреждение о breaking changes

### Автоматическая синхронизация

GitHub Action автоматически запускается каждый день в 9:00 UTC (12:00 МСК) и:
- Проверяет изменения в Figma
- Создает Pull Request с обновлениями
- Отправляет уведомление в Telegram

## 📋 Что синхронизируется

### Компоненты
- ✅ Новые компоненты добавляются автоматически
- ✅ Измененные компоненты обновляются
- ✅ Удаленные компоненты помечаются в changelog

### Токены
- ✅ Colors (цвета)
- ✅ Spacing (отступы)
- ✅ Typography (типографика)
- ✅ Radius (радиусы)
- ✅ Shadows (тени)

## ⚠️ Breaking Changes

Скрипт автоматически обнаруживает потенциальные breaking changes:
- Изменение структуры пропсов компонента
- Удаление компонентов
- Изменение токенов

При обнаружении breaking changes:
1. ⚠️ Выводится предупреждение в консоль
2. 📝 Записывается в `CHANGELOG.md`
3. 🔔 Отправляется уведомление в Telegram (если настроено)

## 📝 Changelog

Все изменения записываются в `docs/design-system/CHANGELOG.md`:

```markdown
## [2025-12-01] - Синхронизация с Figma

### ➕ Добавлено
- Компонент: Button
- Компонент: Card

### ✏️ Изменено
- Компонент: Input (обновлены пропсы)

### 🗑️ Удалено
- Компонент: OldComponent

### ⚠️ Breaking Changes
- Компонент Input: изменена структура пропсов
```

## 🔧 Настройка GitHub Action

### Необходимые Secrets

Добавьте в GitHub Secrets (Settings → Secrets and variables → Actions):

1. `FIGMA_TOKEN` - Personal Access Token из Figma
2. `FIGMA_FILE_ID` - ID файла Figma (опционально, можно в .env)
3. `TELEGRAM_BOT_TOKEN` - Токен Telegram бота (опционально)
4. `TELEGRAM_CHAT_ID` - ID чата для уведомлений (опционально)

### Получение Telegram токена

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Узнайте Chat ID через [@userinfobot](https://t.me/userinfobot)

### Расписание

По умолчанию синхронизация запускается каждый день в 9:00 UTC.

Изменить расписание можно в `.github/workflows/sync-figma.yml`:

```yaml
schedule:
  - cron: '0 9 * * *'  # Каждый день в 9:00 UTC
```

Формат cron: `минута час день месяц день_недели`

Примеры:
- `0 9 * * *` - каждый день в 9:00
- `0 9 * * 1` - каждый понедельник в 9:00
- `0 */6 * * *` - каждые 6 часов

## 🔍 Проверка состояния

Состояние последней синхронизации хранится в `scripts/.figma-sync-state.json`:

```json
{
  "lastModified": "2025-12-01T12:00:28Z",
  "components": {
    "component-id": "2025-12-01T12:00:28Z"
  },
  "tokens": {
    "lastModified": "2025-12-01T12:00:28Z"
  }
}
```

Этот файл используется для определения изменений и не должен коммититься в git.

## 🛠️ Troubleshooting

### Ошибка: FIGMA_TOKEN не установлен

Добавьте токен в `.env` файл:
```env
FIGMA_TOKEN=ваш_токен
FIGMA_FILE_ID=ваш_file_id
```

### Ошибка: 403 Forbidden

Variables API требует Enterprise план. Скрипт продолжит работу со стандартными токенами.

### Компоненты не обновляются

Проверьте:
1. Правильность `FIGMA_FILE_ID`
2. Доступность файла в Figma
3. Валидность `FIGMA_TOKEN`

## 📚 Дополнительная информация

- [Figma API Documentation](https://www.figma.com/developers/api)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Telegram Bot API](https://core.telegram.org/bots/api)



