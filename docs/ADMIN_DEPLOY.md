# Деплой Админки в Yandex Object Storage

Инструкция по развертыванию админ-панели в Yandex Object Storage.

## 🎯 Быстрый старт

### Вариант 1: Автоматический деплой (Рекомендуется)

```bash
npm run deploy:admin
```

Эта команда автоматически:
1. Соберет админку (`npm run build:admin`)
2. Загрузит все файлы в Object Storage bucket `lug-admin-deploy`
3. Покажет URL вашей админ-панели

### Вариант 2: С очисткой бакета

Если нужно полностью очистить бакет перед загрузкой:

```bash
npm run deploy:admin:clear
```

## 📋 Требования

### 1. Создание бакета в Yandex Object Storage

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Перейдите в **Object Storage** → **Buckets**
3. Создайте новый бакет:
   - **Имя:** `lug-admin-deploy` (или любое другое уникальное имя)
   - **Тип доступа:** Публичный (Public)
   - **Регион:** `ru-central1`

### 2. Настройка переменных окружения

Скрипт использует ключи по умолчанию из вашего запроса:
- **Access Key ID:** `YCAJE9rBN49EzJOqjC9LmT4BS`
- **Secret Access Key:** `YCPK0EBU7f-IHoh29XkOt_XJ0D3MYRw6RbO6BjTl`
- **Bucket:** `lug-admin-deploy`

Или создайте файл `.env` в корне проекта:

```env
# Админка
ADMIN_STORAGE_BUCKET_DEV=lug-admin-deploy
ADMIN_STORAGE_ACCESS_KEY_DEV=YCAJE9rBN49EzJOqjC9LmT4BS
ADMIN_STORAGE_SECRET_KEY_DEV=YCPK0EBU7f-IHoh29XkOt_XJ0D3MYRw6RbO6BjTl
```

## 🚀 Процесс деплоя

### Шаг 1: Сборка админки

```bash
npm run build:admin
```

Файлы будут собраны в папку `admin/dist/`

### Шаг 2: Загрузка в Object Storage

```bash
npm run deploy:admin
```

Скрипт автоматически:
- Подключится к Yandex Object Storage
- Загрузит все файлы из `admin/dist/`
- Установит правильные Content-Type для файлов
- Настроит кеширование (assets - долгое, HTML - без кеша)

### Шаг 3: Получение URL

После успешного деплоя вы получите два URL:

1. **Storage URL:** `https://storage.yandexcloud.net/lug-admin-deploy/`
2. **Website URL:** `https://lug-admin-deploy.website.yandexcloud.net/`

**Используйте Website URL** для доступа к админ-панели.

## 🔧 Настройка Website Hosting

Для работы админки через Website URL нужно включить статический хостинг:

1. Откройте бакет в Yandex Cloud Console
2. Перейдите в **Настройки** → **Статический сайт**
3. Включите **Статический сайт**
4. Укажите:
   - **Главная страница:** `index.html`
   - **Страница ошибки:** `index.html` (для SPA)
5. Сохраните настройки

## 📝 Структура файлов

После деплоя в бакете будут:
```
lug-admin-deploy/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ...
└── ...
```

## 🔐 Безопасность

⚠️ **Важно:** Админ-панель должна быть защищена!

### Рекомендации:

1. **Ограничьте доступ по IP:**
   - В настройках бакета добавьте политику доступа
   - Разрешите доступ только с ваших IP-адресов

2. **Используйте CloudFront/CDN:**
   - Настройте CloudFront с аутентификацией
   - Или используйте Yandex Cloud CDN

3. **Добавьте авторизацию в админке:**
   - Реализуйте вход через Telegram или другой метод
   - Храните токены в localStorage

## 🐛 Решение проблем

### Ошибка: "Source directory not found"

**Решение:** Сначала соберите админку:
```bash
npm run build:admin
```

### Ошибка: "Object Storage credentials not found"

**Решение:** Проверьте переменные окружения или используйте значения по умолчанию из скрипта.

### Ошибка: "Access Denied"

**Решение:** 
- Проверьте правильность ключей доступа
- Убедитесь, что бакет существует
- Проверьте права доступа к бакету

### Админка не открывается

**Решение:**
- Убедитесь, что включен статический хостинг
- Проверьте, что `index.html` загружен в корень бакета
- Проверьте настройки CORS (если нужны API запросы)

## 📊 Мониторинг

После деплоя проверьте:
- ✅ Файлы загружены в бакет
- ✅ Статический хостинг включен
- ✅ Website URL открывается
- ✅ API запросы работают (проверьте CORS)

## 🔄 Обновление админки

Для обновления просто запустите деплой снова:

```bash
npm run deploy:admin
```

Или с очисткой:

```bash
npm run deploy:admin:clear
```

## 📚 Дополнительные ресурсы

- [Документация Yandex Object Storage](https://cloud.yandex.ru/docs/storage/)
- [Настройка статического хостинга](https://cloud.yandex.ru/docs/storage/concepts/hosting)

