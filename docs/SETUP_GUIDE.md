# Пошаговое руководство по настройке проекта

Подробная инструкция для тех, кто не является разработчиком. Все шаги расписаны максимально понятно.

## 📋 Содержание

1. [Установка необходимых инструментов](#1-установка-необходимых-инструментов)
2. [Настройка проекта на компьютере](#2-настройка-проекта-на-компьютере)
3. [Настройка переменных окружения](#3-настройка-переменных-окружения)
4. [Настройка Telegram бота](#4-настройка-telegram-бота)
5. [Настройка Yandex Cloud](#5-настройка-yandex-cloud)
6. [Настройка Sentry (мониторинг ошибок)](#6-настройка-sentry-мониторинг-ошибок)
7. [Настройка Yandex Metrica (аналитика)](#7-настройка-yandex-metrica-аналитика)
8. [Настройка GitHub Actions (автоматический деплой)](#8-настройка-github-actions-автоматический-деплой)
9. [Проверка работы](#9-проверка-работы)

---

## 1. Установка необходимых инструментов

### Шаг 1.1: Установка Node.js

Node.js — это программа, которая нужна для запуска проекта.

**Для Mac:**
1. Откройте сайт: https://nodejs.org/
2. Нажмите кнопку "Download" (зеленая кнопка)
3. Скачайте версию LTS (рекомендуемая)
4. Откройте скачанный файл и следуйте инструкциям установки
5. Проверьте установку: откройте "Терминал" (Terminal) и введите:
   ```bash
   node --version
   ```
   Должно показать что-то вроде `v18.17.0` или выше

**Для Windows:**
1. Откройте сайт: https://nodejs.org/
2. Нажмите кнопку "Download" (зеленая кнопка)
3. Скачайте версию LTS (рекомендуемая)
4. Запустите установщик и следуйте инструкциям (всё время нажимайте "Next")
5. Проверьте установку: откройте "Командную строку" (Command Prompt) и введите:
   ```bash
   node --version
   ```
   Должно показать что-то вроде `v18.17.0` или выше

### Шаг 1.2: Установка Git

Git — это программа для работы с кодом.

**Для Mac:**
1. Откройте "Терминал" (Terminal)
2. Введите команду:
   ```bash
   git --version
   ```
3. Если Git не установлен, установите Xcode Command Line Tools:
   ```bash
   xcode-select --install
   ```

**Для Windows:**
1. Откройте сайт: https://git-scm.com/download/win
2. Скачайте установщик
3. Запустите и следуйте инструкциям (можно оставить все настройки по умолчанию)

### Шаг 1.3: Установка редактора кода (опционально, но рекомендуется)

Рекомендуется установить Visual Studio Code:
1. Откройте сайт: https://code.visualstudio.com/
2. Скачайте и установите
3. Это поможет вам редактировать файлы конфигурации

---

## 2. Настройка проекта на компьютере

### Шаг 2.1: Клонирование проекта

1. Откройте "Терминал" (Mac) или "Командную строку" (Windows)
2. Перейдите в папку, где хотите разместить проект (например, Desktop):
   ```bash
   cd ~/Desktop
   ```
3. Если проект уже есть на GitHub, клонируйте его:
   ```bash
   git clone https://github.com/ваш-username/lug.git
   ```
   Или если проект уже на вашем компьютере, просто перейдите в папку:
   ```bash
   cd lug
   ```

### Шаг 2.2: Установка зависимостей

В папке проекта выполните команду:
```bash
npm install
```

Это займет несколько минут. Дождитесь завершения.

**Если возникла ошибка:**
- Убедитесь, что вы в правильной папке (там, где есть файл `package.json`)
- Проверьте, что Node.js установлен: `node --version`

---

## 3. Настройка переменных окружения

Переменные окружения — это секретные данные (токены, пароли), которые нужны для работы приложения.

### Шаг 3.1: Создание файла .env

1. В папке проекта найдите файл `.env.example`
2. Скопируйте его и переименуйте в `.env`
   
   **Через терминал:**
   ```bash
   cp .env.example .env
   ```
   
   **Или вручную:**
   - Скопируйте файл `.env.example`
   - Переименуйте копию в `.env`

### Шаг 3.2: Открытие файла .env

Откройте файл `.env` в любом текстовом редакторе (можно в Visual Studio Code или даже в Блокноте).

Вы увидите что-то вроде:
```bash
NODE_ENV=development
TELEGRAM_BOT_TOKEN_PROD=
TELEGRAM_BOT_TOKEN_DEV=
...
```

### Шаг 3.3: Заполнение базовых переменных

Пока заполните только самое необходимое:

```bash
# Окружение (пока оставьте development)
NODE_ENV=development

# Порт для backend (можно оставить как есть)
PORT=4000
```

Остальные переменные заполним в следующих шагах.

**Важно:** Не коммитьте файл `.env` в Git! Он уже должен быть в `.gitignore`.

---

## 4. Настройка Telegram бота

### Шаг 4.1: Создание бота в BotFather

1. Откройте Telegram
2. Найдите бота `@BotFather`
3. Отправьте команду `/newbot`
4. Введите имя бота (например: "My Dating App")
5. Введите username бота (должен заканчиваться на `bot`, например: `my_dating_app_bot`)
6. BotFather пришлет вам токен, который выглядит так:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
7. **Скопируйте этот токен!** Он понадобится дальше

### Шаг 4.2: Создание второго бота для тестирования (опционально)

Для тестирования лучше создать отдельного бота:
1. Снова отправьте `/newbot` в BotFather
2. Создайте второго бота (например: "My Dating App Test")
3. Скопируйте токен второго бота

### Шаг 4.3: Добавление токенов в .env

Откройте файл `.env` и добавьте токены:

```bash
# Production бот (основной)
TELEGRAM_BOT_TOKEN_PROD=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Development бот (для тестирования)
TELEGRAM_BOT_TOKEN_DEV=9876543210:XYZabcDEFghiJKLmnoPQRstuv
```

**Важно:** Замените токены на реальные, которые вам дал BotFather!

---

## 5. Настройка Yandex Cloud

Yandex Cloud нужен для размещения вашего приложения в интернете.

### Шаг 5.1: Регистрация в Yandex Cloud

1. Откройте сайт: https://cloud.yandex.ru/
2. Нажмите "Начать работу"
3. Зарегистрируйтесь или войдите
4. Создайте платежный аккаунт (нужна банковская карта)

### Шаг 5.2: Создание облака (Cloud)

1. В консоли Yandex Cloud нажмите "Создать облако"
2. Введите название (например: "dating-app")
3. Выберите организацию (или создайте новую)
4. Нажмите "Создать"

### Шаг 5.3: Создание сервисного аккаунта

1. В меню слева выберите "Сервисные аккаунты"
2. Нажмите "Создать сервисный аккаунт"
3. Введите имя: `dating-app-deploy`
4. Нажмите "Создать"
5. Выберите созданный аккаунт
6. Перейдите на вкладку "Ключи"
7. Нажмите "Создать новый ключ" → "JSON"
8. **Скачайте файл!** Это ваш ключ доступа

### Шаг 5.4: Настройка прав сервисного аккаунта

1. Выберите сервисный аккаунт
2. Нажмите "Назначить роли"
3. Добавьте роли:
   - `editor` (редактор)
   - `serverless.functions.invoker` (для Cloud Functions)
   - `storage.uploader` (для Object Storage)

### Шаг 5.5: Создание YDB базы данных

1. В меню выберите "YDB"
2. Нажмите "Создать базу данных"
3. Выберите "Serverless" режим
4. Введите имя: `dating-app-db`
5. Выберите регион (ближайший к вам)
6. Нажмите "Создать"
7. После создания скопируйте:
   - **Endpoint** (адрес базы)
   - **Database** (имя базы)

### Шаг 5.6: Создание Object Storage

1. В меню выберите "Object Storage"
2. Нажмите "Создать bucket"
3. Введите имя: `dating-app-storage`
4. Выберите тип доступа: "Публичный" (для статики) или "Приватный" (для фото)
5. Нажмите "Создать"

### Шаг 5.7: Добавление данных Yandex Cloud в .env

Откройте файл `.env` и добавьте:

```bash
# YDB Database (Production)
YDB_ENDPOINT_PROD=grpcs://ydb.serverless.yandexcloud.net:2135
YDB_DATABASE_PROD=/ru-central1/b1g.../etn.../dating-app-db
YDB_TOKEN_PROD=ваш-токен-из-json-файла

# YDB Database (Development - можно использовать ту же базу или создать отдельную)
YDB_ENDPOINT_DEV=grpcs://ydb.serverless.yandexcloud.net:2135
YDB_DATABASE_DEV=/ru-central1/b1g.../etn.../dating-app-db
YDB_TOKEN_DEV=ваш-токен-из-json-файла

# Object Storage
OBJECT_STORAGE_URL=https://storage.yandexcloud.net/dating-app-storage
```

**Где взять токен:**
- Откройте скачанный JSON файл сервисного аккаунта
- Найдите поле `service_account_key` → `id` и `private_key`
- Или используйте IAM токен (временный, нужно обновлять)

---

## 6. Настройка мониторинга ошибок (для России)

**Важно:** Sentry недоступен в России. Используем альтернативы, которые работают без VPN.

### Вариант А: Yandex AppMetrica (рекомендуется)

**Преимущества:** Работает в России, можно оплатить русской картой, бесплатный тариф.

#### Шаг 6.1: Регистрация в Yandex AppMetrica

1. Откройте сайт: https://appmetrica.yandex.ru/
2. Войдите через Яндекс аккаунт
3. Нажмите "Создать приложение"
4. Заполните:
   - Название: "Dating App"
   - Платформа: "Web"
5. **Скопируйте API ключ** (номер счетчика, например: `12345678`)

#### Шаг 6.2: Добавление в .env

Откройте файл `.env` и добавьте:

```bash
# Yandex AppMetrica (мониторинг ошибок и аналитика)
VITE_YANDEX_METRICA_ID=12345678
```

**Где взять:** https://appmetrica.yandex.ru/

### Вариант Б: Catcher (российский аналог Sentry)

**Преимущества:** Полный аналог Sentry, работает в России, можно оплатить русской картой.

#### Шаг 6.1: Регистрация в Catcher

1. Откройте сайт: https://catcher.io/
2. Зарегистрируйтесь
3. Создайте проект
4. **Скопируйте API ключ и Project ID**

#### Шаг 6.2: Добавление в .env

Откройте файл `.env` и добавьте:

```bash
# Catcher (мониторинг ошибок)
VITE_CATCHER_API_KEY=ваш-api-ключ
VITE_CATCHER_PROJECT_ID=ваш-project-id
CATCHER_API_KEY=ваш-api-ключ
CATCHER_PROJECT_ID=ваш-project-id
```

**Где взять:** https://catcher.io/

**Подробнее:** См. `docs/MONITORING_RU.md`

---

## 7. Настройка Yandex Metrica (аналитика)

Yandex Metrica помогает отслеживать поведение пользователей.

### Шаг 7.1: Создание счетчика

1. Откройте сайт: https://metrika.yandex.ru/
2. Войдите через Яндекс аккаунт
3. Нажмите "Добавить счетчик"
4. Заполните:
   - Название: "Dating App"
   - Адрес сайта: `https://app.yourdomain.com` (или временный)
5. Примите условия и нажмите "Создать счетчик"
6. **Скопируйте номер счетчика** (это число, например: `12345678`)

### Шаг 7.2: Добавление номера счетчика в .env

Откройте файл `.env` и добавьте:

```bash
# Yandex Metrica
VITE_YANDEX_METRICA_ID=12345678
```

**Важно:** Замените на реальный номер вашего счетчика!

---

## 8. Настройка GitHub Actions (автоматический деплой)

GitHub Actions автоматически разворачивает ваше приложение при обновлении кода.

### Шаг 8.1: Создание репозитория на GitHub

1. Откройте сайт: https://github.com/
2. Войдите в аккаунт
3. Нажмите "New repository" (новый репозиторий)
4. Введите название: `lug` (или другое)
5. Выберите "Private" (приватный) или "Public" (публичный)
6. **НЕ** ставьте галочки на "Initialize with README"
7. Нажмите "Create repository"

### Шаг 8.2: Загрузка кода на GitHub

В терминале выполните:

```bash
# Если Git еще не инициализирован
git init

# Добавить все файлы
git add .

# Создать первый коммит
git commit -m "Initial commit"

# Подключить к GitHub (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/lug.git

# Отправить код на GitHub
git branch -M main
git push -u origin main
```

### Шаг 8.3: Настройка GitHub для автоматического деплоя

Для автоматического деплоя в Yandex Cloud нужно настроить:

1. **Secrets** - секретные ключи (YC_SERVICE_ACCOUNT_KEY)
2. **Environments** - окружения (staging, production)

📖 **Подробная инструкция:** [docs/GITHUB_SETUP.md](GITHUB_SETUP.md)

#### Краткая инструкция:

**1. Создание Secret:**
- Откройте: https://github.com/vidmichd-lab/lug-date/settings/secrets/actions
- Нажмите **New repository secret**
- Name: `YC_SERVICE_ACCOUNT_KEY`
- Secret: скопируйте весь JSON файл сервисного аккаунта Yandex Cloud

**2. Создание Environments:**
- Откройте: https://github.com/vidmichd-lab/lug-date/settings/environments
- Создайте `staging` (для ветки develop)
- Создайте `production` (для ветки main, с ограничением на main ветку)

**3. Проверка:**
- Secrets: должен быть виден `YC_SERVICE_ACCOUNT_KEY`
- Environments: должны быть видны `staging` и `production`

После настройки GitHub Actions автоматически будет деплоить:
- В `staging` при пуше в ветку `develop`
- В `production` при пуше в ветку `main`

---

## 9. Проверка работы

### Шаг 9.1: Локальный запуск

1. Убедитесь, что файл `.env` заполнен
2. В терминале выполните:
   ```bash
   npm run seed:dev
   ```
   Это создаст тестовые данные

3. Запустите приложение:
   ```bash
   NODE_ENV=development npm run dev:all
   ```

4. Откройте в браузере:
   - Frontend: http://localhost:3000
   - Backend: http://localhost:4000/health
   - Admin: http://localhost:5174

### Шаг 9.2: Проверка работы бота

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветствием

### Шаг 9.3: Проверка Sentry

1. Откройте приложение
2. Создайте тестовую ошибку (если возможно)
3. Проверьте в Sentry, появилась ли ошибка

### Шаг 9.4: Проверка Yandex Metrica

1. Откройте приложение в браузере
2. Подождите несколько минут
3. Проверьте в Yandex Metrica, появились ли визиты

---

## ❓ Частые проблемы и решения

### Проблема: "npm: command not found"

**Решение:** Node.js не установлен или не добавлен в PATH. Переустановите Node.js.

### Проблема: "Cannot find module"

**Решение:** Зависимости не установлены. Выполните:
```bash
npm install
```

### Проблема: "Port already in use"

**Решение:** Порт занят другим приложением. Измените порт в `.env`:
```bash
PORT=4001
```

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте токен в `.env`
2. Убедитесь, что бот запущен: `npm run dev:bot`
3. Проверьте логи на ошибки

### Проблема: Ошибки при деплое

**Решение:**
1. Проверьте секреты в GitHub
2. Убедитесь, что сервисный аккаунт имеет нужные права
3. Проверьте логи в GitHub Actions

---

## 📞 Получение помощи

Если что-то не работает:

1. Проверьте логи в терминале
2. Проверьте файл `.env` (все ли заполнено)
3. Посмотрите документацию в папке `docs/`
4. Создайте Issue на GitHub с описанием проблемы

---

## ✅ Чек-лист готовности

Отметьте выполненные шаги:

- [ ] Node.js установлен
- [ ] Git установлен
- [ ] Проект склонирован/настроен
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` создан и заполнен
- [ ] Telegram бот создан, токены добавлены
- [ ] Yandex Cloud настроен
- [ ] Sentry настроен, DSN добавлены
- [ ] Yandex Metrica настроена, ID добавлен
- [ ] GitHub репозиторий создан
- [ ] GitHub Actions секреты добавлены
- [ ] Приложение запускается локально
- [ ] Бот отвечает на команды

**Когда все отмечено — проект готов к работе!** 🎉

---

**Последнее обновление:** 2024-01-01

