# Настройка Yandex AppMetrica

Документ описывает настройку Yandex AppMetrica для мониторинга и аналитики.

## ✅ Данные для настройки

**AppMetrica ID:** `4809916`  
**API key (для SDK):** `3fdf8d82-1c23-413e-8870-75bbac42ebd7`  
**Post API key:** `700c6ca2-4e4f-4447-922b-b864e0072559`

## 📝 Настройка в проекте

### 1. Добавьте переменную в `.env` файл

Добавьте следующую строку в корневой `.env` файл:

```bash
# Yandex AppMetrica
VITE_YANDEX_METRICA_ID=4809916
```

### 2. Проверка настройки

После добавления переменной:

1. Перезапустите dev сервер:
   ```bash
   npm run dev:frontend
   ```

2. Откройте консоль браузера - вы должны увидеть:
   ```
   ✅ Yandex Metrica initialized
   ✅ Yandex AppMetrica ready for error tracking
   ✅ Error monitoring initialized for development environment
   ```

3. Проверьте в AppMetrica:
   - Откройте https://appmetrica.yandex.ru/
   - Войдите в ваш проект
   - Проверьте, что события начинают поступать

## 🎯 Что отслеживается

### Автоматически:
- Ошибки JavaScript (через global error handlers)
- Необработанные Promise rejections
- События пользователей (через `reportEvent`)

### Вручную через код:
```typescript
import { reportEvent, reportError } from './monitoring';
import { trackMatch, trackLike, trackPhotoUpload } from './utils/metrica';

// Отслеживание событий
reportEvent('user_registered', { userId: '123' });

// Отслеживание ошибок
try {
  // ваш код
} catch (error) {
  reportError(error, { component: 'UserProfile' });
}

// Специальные события
trackMatch(matchId, eventId);
trackLike(targetUserId, eventId);
trackPhotoUpload(success);
```

## 📊 Просмотр данных

1. Откройте https://appmetrica.yandex.ru/
2. Выберите ваш проект (ID: 4809916)
3. Перейдите в разделы:
   - **Ошибки** - просмотр JavaScript ошибок
   - **События** - просмотр пользовательских событий
   - **Аналитика** - общая статистика

## 🔧 Использование API key

API key (`3fdf8d82-1c23-413e-8870-75bbac42ebd7`) можно использовать для:
- Отправки событий через REST API
- Интеграции с backend (если нужно)

Post API key (`700c6ca2-4e4f-4447-922b-b864e0072559`) используется для:
- Отправки данных через POST запросы

**Примечание:** Для frontend приложения достаточно только ID счетчика. API keys используются для backend интеграции (если потребуется).

## ✅ Проверка работы

1. Откройте приложение в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте, что нет ошибок инициализации
4. Выполните какое-либо действие в приложении
5. Проверьте в AppMetrica, что события поступают

## 🚨 Устранение проблем

### AppMetrica не инициализируется
- Проверьте, что `VITE_YANDEX_METRICA_ID` установлен в `.env`
- Перезапустите dev сервер после изменения `.env`
- Проверьте консоль браузера на ошибки

### События не поступают
- Проверьте, что ID правильный (4809916)
- Убедитесь, что нет блокировщиков рекламы (они могут блокировать AppMetrica)
- Проверьте Network tab в DevTools - должны быть запросы к `mc.yandex.ru`

### Ошибки в консоли
- Проверьте, что скрипт AppMetrica загружается
- Убедитесь, что нет конфликтов с другими скриптами

---

**Дата настройки:** 2024-12-01  
**AppMetrica ID:** 4809916



