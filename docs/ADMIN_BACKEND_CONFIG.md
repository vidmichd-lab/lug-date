# Настройка Backend URL для Админки

Админка использует runtime конфигурацию для определения URL бэкенда. Это позволяет менять URL без пересборки приложения.

## 🔧 Настройка

### Вариант 1: Через переменную окружения при деплое

При деплое админки установите переменную окружения `BACKEND_URL`:

```bash
BACKEND_URL=https://your-backend-url.com npm run deploy:admin
```

Или добавьте в `.env`:

```env
BACKEND_URL=https://your-backend-url.com
```

Скрипт деплоя автоматически обновит `config.js` с правильным URL.

### Вариант 2: Ручное редактирование config.js

После сборки, но перед деплоем, отредактируйте файл `admin/dist/config.js`:

```javascript
window.ADMIN_CONFIG = window.ADMIN_CONFIG || {
  API_URL: 'https://your-backend-url.com'
};
```

### Вариант 3: Через window.ADMIN_CONFIG (для разработки)

В консоли браузера или через другой скрипт:

```javascript
window.ADMIN_CONFIG = {
  API_URL: 'https://your-backend-url.com'
};
```

Это должно быть установлено **до** загрузки основного скрипта приложения.

## 📋 Приоритет конфигурации

Админка использует URL в следующем порядке приоритета:

1. `window.ADMIN_CONFIG.API_URL` (runtime, самый высокий приоритет)
2. `import.meta.env.VITE_API_URL` (переменная окружения при сборке)
3. `http://localhost:4000` (fallback для разработки)

## 🚀 Примеры

### Для локальной разработки

```bash
# В .env файле
VITE_API_URL=http://localhost:4000
```

### Для продакшена

```bash
# При деплое
BACKEND_URL=https://api.yourdomain.com npm run deploy:admin
```

### Для разных окружений

```bash
# Development
BACKEND_URL=http://localhost:4000 npm run deploy:admin

# Staging
BACKEND_URL=https://staging-api.yourdomain.com npm run deploy:admin

# Production
BACKEND_URL=https://api.yourdomain.com npm run deploy:admin
```

## ⚠️ Важно

- Убедитесь, что backend доступен по указанному URL
- Проверьте настройки CORS в backend (`backend/src/index.ts`)
- Для продакшена используйте HTTPS
- После изменения `config.js` нужно пересобрать и задеплоить админку

## 🔍 Проверка

После деплоя откройте консоль браузера и проверьте:

```javascript
console.log(window.ADMIN_CONFIG);
```

Должен показать объект с `API_URL`.

