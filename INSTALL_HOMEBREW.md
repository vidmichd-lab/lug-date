# 🍺 Установка Homebrew и дополнительных инструментов

## ⚠️ Важно

Установка Homebrew требует **прав администратора** и **интерактивного ввода пароля**. 

## 📋 Инструкция по установке Homebrew

### Способ 1: Автоматическая установка (рекомендуется)

Откройте терминал и выполните:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Следуйте инструкциям на экране. Вам будет предложено:
1. Ввести пароль администратора
2. Нажать Enter для подтверждения установки

### Способ 2: Через скрипт проекта

```bash
cd /Users/timitro/Downloads/lug
./scripts/install-all-tools.sh
```

---

## 🔧 После установки Homebrew

### Добавление в PATH

После установки выполните команды, которые будут показаны на экране. Обычно это:

**Для Apple Silicon (M1/M2/M3):**
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

**Для Intel Mac:**
```bash
echo 'eval "$(/usr/local/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/usr/local/bin/brew shellenv)"
```

### Проверка установки

```bash
brew --version
```

---

## 📦 Установка полезных инструментов через Homebrew

После установки Homebrew можно установить дополнительные инструменты:

### Базовые инструменты:

```bash
brew install wget        # Улучшенный curl
brew install jq          # JSON processor
brew install tree        # Показывает структуру директорий
brew install htop        # Улучшенный top
brew install watch       # Выполняет команду периодически
brew install git-lfs      # Git Large File Storage
```

### GUI приложения (опционально):

```bash
brew install --cask visual-studio-code  # VS Code
brew install --cask iterm2              # Улучшенный терминал
brew install --cask postman             # API тестирование
brew install --cask docker               # Docker Desktop (если еще не установлен)
```

---

## ✅ Что уже установлено

- ✅ **Node.js** (v20.11.0) - установлен в `~/nodejs/bin/`
- ✅ **Docker** (29.1.2) - установлен и работает
- ✅ **Yandex Cloud CLI** (0.180.0) - установлен и настроен
- ✅ **Git** - установлен в системе

---

## 🚀 Быстрая установка всех инструментов

После установки Homebrew выполните:

```bash
cd /Users/timitro/Downloads/lug
./scripts/install-all-tools.sh
```

Скрипт автоматически установит:
- wget
- jq
- tree
- htop
- watch
- И другие полезные инструменты

---

## 📚 Полезные команды Homebrew

```bash
brew update              # Обновить Homebrew
brew upgrade             # Обновить все установленные пакеты
brew list                # Список установленных пакетов
brew search <package>    # Поиск пакета
brew info <package>      # Информация о пакете
brew uninstall <package> # Удалить пакет
```

---

## ⚡ Альтернатива: Установка без Homebrew

Если не хотите устанавливать Homebrew, можно установить инструменты вручную:

### jq (JSON processor):
```bash
cd /tmp
curl -L https://github.com/jqlang/jq/releases/download/jq-1.7.1/jq-macos-arm64 -o ~/jq
chmod +x ~/jq
echo 'export PATH=$HOME:$PATH' >> ~/.zprofile
```

### wget:
Скачайте с: https://www.gnu.org/software/wget/

---

## 💡 Рекомендация

**Установите Homebrew** - это самый удобный способ управления пакетами на macOS. Он упростит установку и обновление всех инструментов в будущем.

---

## 📝 Примечание

Если у вас нет прав администратора или возникают проблемы с установкой, можно использовать уже установленные инструменты:
- Node.js (установлен)
- Docker (установлен)
- YC CLI (установлен)
- Git (установлен)

Этих инструментов достаточно для работы с проектом!

