# 🚀 Как задеплоить Frontend

## Проблема: Workflow не виден в GitHub Actions

Если вы не видите workflow "Deploy Frontend to Yandex Object Storage" в GitHub Actions, это нормально - он появится после первого запуска.

## ✅ Решение: Запустить workflow вручную

### Шаг 1: Откройте GitHub Actions

1. Перейдите на: https://github.com/vidmichd-lab/lug-date/actions
2. Если видите список workflows - отлично, переходите к Шагу 2
3. Если не видите - нажмите на вкладку **"Actions"** в верхнем меню репозитория

### Шаг 2: Найдите workflow

1. В списке workflows найдите **"Deploy Frontend to Yandex Object Storage"**
2. Если его нет в списке:
   - Нажмите на вкладку **"All workflows"** (Все workflows)
   - Или нажмите **"Get started"** если это первый раз
   - Workflow должен появиться после того, как вы его запустите

### Шаг 3: Запустите workflow вручную

1. Нажмите на workflow **"Deploy Frontend to Yandex Object Storage"**
2. Справа вверху нажмите кнопку **"Run workflow"** (Запустить workflow)
3. Выберите:
   - **Branch:** `develop` (для staging) или `main` (для production)
   - Нажмите зеленую кнопку **"Run workflow"**

### Шаг 4: Проверьте статус

1. После запуска вы увидите новый запуск workflow
2. Нажмите на него, чтобы увидеть прогресс
3. Дождитесь завершения (обычно 2-3 минуты)

### Шаг 5: Получите URL frontend

После успешного деплоя:
1. В логах workflow найдите строку: `🌐 Website URL: https://...`
2. Скопируйте этот URL
3. Это и есть URL вашего frontend приложения

---

## ⚠️ Если workflow падает с ошибкой

### Ошибка: "Secrets not found"

**Решение:** Добавьте секреты в GitHub:

1. Перейдите: https://github.com/vidmichd-lab/lug-date/settings/secrets/actions
2. Нажмите **"New repository secret"**
3. Добавьте следующие секреты:

   **Для staging (develop):**
   - Name: `FRONTEND_STORAGE_BUCKET_DEV`
     Value: имя вашего бакета (например: `dating-app-storage`)
   
   - Name: `FRONTEND_STORAGE_ACCESS_KEY_DEV`
     Value: ваш Access Key ID
   
   - Name: `FRONTEND_STORAGE_SECRET_KEY_DEV`
     Value: ваш Secret Access Key

   **Для production (main):**
   - Name: `FRONTEND_STORAGE_BUCKET_PROD`
   - Name: `FRONTEND_STORAGE_ACCESS_KEY_PROD`
   - Name: `FRONTEND_STORAGE_SECRET_KEY_PROD`

4. После добавления секретов запустите workflow снова

### Ошибка: "Bucket not found"

**Решение:** Убедитесь, что:
- Бакет существует в Yandex Object Storage
- Имя бакета в секрете совпадает с реальным именем
- Бакет имеет публичный доступ

---

## 🔄 Альтернативный способ: Деплой через терминал

Если GitHub Actions не работает, можно задеплоить локально:

1. Убедитесь, что в `.env` правильные ключи:
   ```env
   FRONTEND_STORAGE_BUCKET_DEV=ваш-бакет
   FRONTEND_STORAGE_ACCESS_KEY_DEV=ваш-ключ
   FRONTEND_STORAGE_SECRET_KEY_DEV=ваш-секрет
   ```

2. Запустите:
   ```bash
   npm run deploy:frontend
   ```

3. После успешного деплоя получите URL из вывода команды

---

## 📱 После деплоя: Настройка Telegram бота

1. Скопируйте URL frontend (из логов workflow или терминала)
2. Откройте BotFather в Telegram
3. Отправьте: `/setmenubutton`
4. Выберите вашего бота
5. Вставьте URL frontend
6. Готово! Приложение будет доступно в боте

---

## ✅ Чеклист

- [ ] Workflow запущен в GitHub Actions
- [ ] Деплой завершился успешно
- [ ] URL frontend получен
- [ ] URL добавлен в BotFather
- [ ] Приложение открывается в Telegram боте

---

**Важно:** Workflow может не отображаться в списке, пока вы его не запустите хотя бы один раз. После первого запуска он появится в истории.



