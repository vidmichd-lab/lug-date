# Диагностика ошибки 403 в админке

## Шаг 1: Проверьте, что именно возвращает бекенд

Откройте DevTools → Network и найдите запрос, который возвращает 403. Проверьте Response:

### Если код ошибки `CORS_ERROR`:

```json
{
  "success": false,
  "error": {
    "message": "Origin not allowed",
    "code": "CORS_ERROR"
  }
}
```

**Решение:** Проблема в CORS. Нужно добавить URL админки в `ADMIN_ORIGINS_PROD`:

- Откройте GitHub → Settings → Secrets and variables → Actions
- Найдите `ADMIN_ORIGINS_PROD`
- Убедитесь, что там указан: `https://lug-admin-deploy.website.yandexcloud.net`
- Передеплойте бекенд

### Если код ошибки `UNAUTHORIZED`:

```json
{
  "success": false,
  "error": {
    "message": "Authorization token required",
    "code": "UNAUTHORIZED"
  }
}
```

**Решение:** Токен не отправляется. Проверьте:

1. Откройте DevTools → Network
2. Найдите запрос к `/api/admin/management/...`
3. Проверьте Request Headers → должно быть: `Authorization: Bearer <токен>`
4. Если нет - проверьте, что вы залогинились и токен сохранен в localStorage

## Шаг 2: Проверьте токен в localStorage

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверить токен
const token = localStorage.getItem('admin_token');
console.log('Token:', token);

// Если токена нет или он пустой - очистить и перезагрузить
if (!token || token.trim().length === 0) {
  localStorage.clear();
  location.reload();
}
```

## Шаг 3: Проверьте логи бекенда

В Yandex Cloud Console проверьте логи бекенда. Найдите записи:

- `admin_auth_check` - показывает, проверяется ли авторизация
- `admin_auth_failed` - показывает причину ошибки
- `cors_check` - показывает проверку CORS
- `cors_blocked` - показывает заблокированные запросы

## Шаг 4: Проверьте переменные окружения

Убедитесь, что в GitHub Secrets настроены:

- `ADMIN_ORIGINS_PROD` = `https://lug-admin-deploy.website.yandexcloud.net`
- `ADMIN_TOKEN_PROD` = (опционально, по умолчанию используется `admin-secret-token-change-in-production`)

**Важно:** Если вы указали `ADMIN_TOKEN_PROD` в секретах, убедитесь, что токен, который вы получаете при логине, совпадает с этим значением.

## Шаг 5: Проверьте логин

1. Откройте админку
2. Откройте консоль браузера (F12)
3. Попробуйте залогиниться
4. Проверьте, что в консоли есть: `✅ Login successful, token received: ...`
5. Проверьте, что токен сохранен: `localStorage.getItem('admin_token')`

## Шаг 6: Проверьте запросы после логина

После успешного логина, при каждом запросе в консоли должно быть:

```
🔑 Adding auth token to request: /api/admin/management/...
```

Если этого нет - токен не отправляется.

## Быстрое решение

Если ничего не помогает:

1. Очистите localStorage:

```javascript
localStorage.clear();
location.reload();
```

2. Залогиньтесь снова:

- Username: `admin`
- Password: `admin123`

3. Проверьте, что токен сохранен:

```javascript
localStorage.getItem('admin_token');
```

4. Проверьте запросы в Network - должен быть заголовок `Authorization: Bearer ...`
