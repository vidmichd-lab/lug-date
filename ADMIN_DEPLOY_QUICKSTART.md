# 🚀 Быстрый деплой админки

## Ваши ключи для деплоя

```
Bucket: lug-admin-deploy
Access Key ID: YCAJE...
Secret Access Key: YCPK...
```

## Шаг 1: Создайте бакет в Yandex Cloud

1. Откройте https://console.cloud.yandex.ru/
2. Object Storage → Buckets → Создать bucket
3. Имя: `lug-admin-deploy`
4. Доступ: Публичный
5. Регион: `ru-central1`

## Шаг 2: Соберите и задеплойте

```bash
# Собрать админку
npm run build:admin

# Задеплоить (использует ключи по умолчанию)
npm run deploy:admin
```

## Шаг 3: Включите статический хостинг

1. Откройте бакет `lug-admin-deploy`
2. Настройки → Статический сайт
3. Включите "Статический сайт"
4. Главная страница: `index.html`
5. Страница ошибки: `index.html`

## Шаг 4: Откройте админку

URL: `https://lug-admin-deploy.website.yandexcloud.net/`

---

## ⚠️ Текущее состояние админки

### ✅ Что работает:
- Аналитика (дашборд с графиками)
- Статистика пользователей, событий, матчей
- Экспорт данных

### 🔨 Что нужно доработать:
1. **Управление пользователями** - API готов, нужны страницы в админке
2. **Управление событиями** - API готов, нужны страницы в админке  
3. **Управление категориями** - API готов, нужны страницы в админке
4. **Настройки приложения** - API готов, нужны страницы в админке

### 📝 API endpoints готовы:
- `GET /api/admin/management/users` - список пользователей
- `GET /api/admin/management/users/:id` - пользователь по ID
- `PATCH /api/admin/management/users/:id` - обновить пользователя
- `DELETE /api/admin/management/users/:id` - заблокировать пользователя
- `GET /api/admin/management/events` - список событий
- `POST /api/admin/management/events` - создать событие
- `PATCH /api/admin/management/events/:id` - обновить событие
- `DELETE /api/admin/management/events/:id` - удалить событие
- `GET /api/admin/management/categories` - список категорий
- `PUT /api/admin/management/categories` - обновить категории
- `GET /api/admin/management/settings` - настройки приложения
- `PUT /api/admin/management/settings` - обновить настройки

---

## 🔧 Исправление ошибок сборки

Перед деплоем нужно исправить ошибки компиляции в админке. Создайте недостающие CSS файлы или обновите импорты.

