# 🔍 Как узнать реальный URL frontend после деплоя

## Проблема: В логах показывается `***.website.yandexcloud.net/`

GitHub Actions скрывает секреты в логах, поэтому имя бакета заменяется на `***`. Это нормально!

## ✅ Способы узнать реальный URL:

### Способ 1: Проверить секрет в GitHub

1. Откройте: https://github.com/vidmichd-lab/lug-date/settings/secrets/actions
2. Найдите секрет `FRONTEND_STORAGE_BUCKET_DEV`
3. Скопируйте значение (это имя бакета)
4. URL будет: `https://[имя-бакета].website.yandexcloud.net/`

**Пример:**

- Если секрет = `dating-app-storage`
- URL = `https://dating-app-storage.website.yandexcloud.net/`

### Способ 2: Проверить в Yandex Cloud Console

1. Откройте: https://console.cloud.yandex.ru/
2. Перейдите в **Object Storage** → **Buckets**
3. Найдите бакет, который используется для frontend
4. Откройте бакет
5. URL будет: `https://[имя-бакета].website.yandexcloud.net/`

### Способ 3: Проверить логи деплоя (после исправления)

После следующего деплоя в логах будет показан правильный URL (после исправления workflow).

### Способ 4: Проверить, куда загрузились файлы

1. Откройте Yandex Cloud Console → Object Storage → Buckets
2. Найдите бакет, в котором есть файлы `index.html`, `assets/` и т.д.
3. Это и есть ваш frontend бакет
4. URL = `https://[имя-бакета].website.yandexcloud.net/`

---

## 📋 Частые имена бакетов:

- `dating-app-storage` - если использовали это имя
- `telegram-app-frontend` - если использовали это имя
- `lug-frontend` - если создали свой бакет
- Другое имя, которое вы указали в секрете `FRONTEND_STORAGE_BUCKET_DEV`

---

## ✅ После того, как узнали URL:

1. Скопируйте URL (например: `https://dating-app-storage.website.yandexcloud.net/`)
2. Откройте BotFather в Telegram
3. Отправьте: `/setmenubutton`
4. Выберите вашего бота
5. Вставьте URL
6. Готово! Приложение будет доступно в боте

---

## 🔧 Если URL не работает:

1. Проверьте, что бакет имеет **публичный доступ**
2. Проверьте, что в бакете есть файл `index.html` в корне
3. Проверьте настройку "Индексный документ" в настройках бакета (должен быть `index.html`)
