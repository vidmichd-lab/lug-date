# 🔧 Исправление проблемы деплоя админки

## Проблема

Ошибка: `The request signature we calculated does not match the signature you provided`

Это означает, что ключи доступа неправильные или бакет не существует.

## ✅ Решение

### Вариант 1: Создать бакет и ключи в Yandex Cloud Console

1. **Откройте Yandex Cloud Console:**
   - https://console.cloud.yandex.ru/
   - Войдите в аккаунт

2. **Создайте бакет:**
   - Object Storage → Buckets → Создать bucket
   - Имя: `lug-admin-deploy`
   - Доступ: **Публичный**
   - Регион: `ru-central1`
   - Нажмите "Создать"

3. **Создайте ключи доступа:**
   - Object Storage → Service accounts
   - Выберите сервисный аккаунт (или создайте новый)
   - Перейдите в "Ключи доступа"
   - Создайте "Статический ключ доступа"
   - Скопируйте:
     - **Access Key ID**
     - **Secret Access Key**

4. **Настройте переменные окружения:**
   
   Создайте или обновите файл `.env` в корне проекта:
   ```env
   ADMIN_STORAGE_BUCKET_DEV=lug-admin-deploy
   ADMIN_STORAGE_ACCESS_KEY_DEV=ВАШ_ACCESS_KEY_ID
   ADMIN_STORAGE_SECRET_KEY_DEV=ВАШ_SECRET_ACCESS_KEY
   ```

5. **Задеплойте:**
   ```bash
   npm run deploy:admin
   ```

### Вариант 2: Использовать существующие ключи

Если у вас уже есть ключи для другого бакета:

1. **Проверьте, что бакет существует:**
   - Откройте Object Storage в консоли
   - Убедитесь, что бакет `lug-admin-deploy` создан

2. **Проверьте права доступа:**
   - Ключи должны иметь права на запись в бакет
   - Сервисный аккаунт должен иметь роль `storage.editor` или `storage.admin`

3. **Обновите `.env`:**
   ```env
   ADMIN_STORAGE_BUCKET_DEV=lug-admin-deploy
   ADMIN_STORAGE_ACCESS_KEY_DEV=YCAJE9rBN49EzJOqjC9LmT4BS
   ADMIN_STORAGE_SECRET_KEY_DEV=YCPK0EBU7f-IHoh29XkOt_XJ0D3MYRw6RbO6BjTl
   ```

4. **Попробуйте снова:**
   ```bash
   npm run deploy:admin
   ```

### Вариант 3: Ручная загрузка через консоль

Если автоматический деплой не работает:

1. **Соберите админку:**
   ```bash
   npm run build:admin
   ```

2. **Откройте бакет в Yandex Cloud Console**

3. **Загрузите файлы вручную:**
   - Откройте бакет `lug-admin-deploy`
   - Нажмите "Загрузить"
   - Выберите все файлы из `admin/dist/`
   - Загрузите их в корень бакета

4. **Включите статический хостинг:**
   - Настройки → Статический сайт
   - Включите "Статический сайт"
   - Главная страница: `index.html`
   - Страница ошибки: `index.html`

## 🔍 Проверка

После деплоя проверьте:

1. **Файлы загружены:**
   - Откройте бакет в консоли
   - Убедитесь, что все файлы на месте

2. **Статический хостинг включен:**
   - Настройки → Статический сайт
   - Должен быть включен

3. **Админка открывается:**
   - URL: `https://lug-admin-deploy.website.yandexcloud.net/`
   - Должна открыться админ-панель

## 🐛 Если все еще не работает

### Проверьте ключи:
```bash
# Проверьте, что ключи правильные
echo $ADMIN_STORAGE_ACCESS_KEY_DEV
echo $ADMIN_STORAGE_SECRET_KEY_DEV
```

### Проверьте бакет:
- Убедитесь, что бакет существует
- Проверьте, что имя бакета правильное
- Убедитесь, что бакет публичный

### Проверьте права:
- Сервисный аккаунт должен иметь роль `storage.editor`
- Ключи должны быть активны

### Альтернатива - используйте другой бакет:
Если `lug-admin-deploy` не работает, создайте новый бакет с другим именем и обновите `.env`.

