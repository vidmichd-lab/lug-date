# 🔧 Исправление подключения к YDB

## ✅ Внесенные изменения

### 1. Добавлена автоматическая переподключение
- Если БД не подключена при запросе, система пытается переподключиться
- Улучшено логирование процесса подключения

### 2. Улучшено логирование
- Добавлены детальные логи процесса подключения
- Логирование ошибок с stack trace

## 🔍 Проверка подключения

### Текущая конфигурация:
- **Endpoint:** `grpcs://ydb.serverless.yandexcloud.net:2135`
- **Database:** `/ru-central1/b1g6a1tnrohoeas9v0k6/etn8n7ptmkui9808eo6b`
- **Service Account Key:** `yc-service-account-key.json` ✅

### Возможные проблемы:

1. **Таймаут подключения (30 секунд)**
   - YDB может быть недоступна
   - Проблемы с сетью
   - Неправильные credentials

2. **Service Account не имеет прав**
   - Нужны права `ydb.editor` или `ydb.admin` на базу данных

3. **Неправильный endpoint или database path**
   - Проверить в Yandex Cloud Console

## 🔧 Решение

### Вариант 1: Проверить подключение вручную

```bash
# Проверить миграции
npm run migrate:status

# Если не работает, проверить логи
# Backend логи покажут детали ошибки подключения
```

### Вариант 2: Проверить права Service Account

1. Откройте Yandex Cloud Console
2. Перейдите в Service Accounts
3. Найдите service account из ключа
4. Проверьте права на YDB базу данных

### Вариант 3: Использовать токен вместо service account

Добавьте в `.env`:
```env
YDB_TOKEN_DEV=ваш_токен
```

## 📊 Текущий статус

Backend запущен с автоматической переподключением. При первом запросе к БД система попытается подключиться автоматически.

Если подключение не происходит:
1. Проверьте логи backend
2. Проверьте права service account
3. Проверьте доступность YDB endpoint


