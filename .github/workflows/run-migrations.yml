name: Run Database Migrations

on:
  workflow_dispatch: # Ручной запуск через кнопку "Run workflow"
  push:
    branches: [main]
    paths:
      - 'backend/src/db/migrations/**'
      - '.github/workflows/run-migrations.yml'

env:
  NODE_VERSION: '20'

jobs:
  run-migrations:
    name: Run database migrations
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      id-token: write
    environment:
      name: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build --workspace=shared

      - name: Run migrations
        timeout-minutes: 10
        env:
          YC_SERVICE_ACCOUNT_KEY: ${{ secrets.YC_SERVICE_ACCOUNT_KEY }}
          YDB_ENDPOINT: ${{ secrets.YDB_ENDPOINT }}
          YDB_DATABASE: ${{ secrets.YDB_DATABASE }}
        run: |
          cd backend

          # Создаём временный файл с Service Account ключом
          echo "$YC_SERVICE_ACCOUNT_KEY" > /tmp/yc-sa-key.json
          chmod 600 /tmp/yc-sa-key.json

          # Используем YC_SERVICE_ACCOUNT_KEY (JSON строка) напрямую
          # Это имеет приоритет над файлом и metadata service в getCredentialsFromEnv
          export YC_SERVICE_ACCOUNT_KEY="$YC_SERVICE_ACCOUNT_KEY"

          # Также создаём файл как fallback
          export YC_SERVICE_ACCOUNT_KEY_FILE="/tmp/yc-sa-key.json"

          echo "✅ Using YC_SERVICE_ACCOUNT_KEY (JSON string) - has priority over metadata service"

          # Явно отключаем metadata service (недоступен в GitHub Actions)
          # YDB SDK проверяет эти переменные перед обращением к metadata service
          export YDB_METADATA_CREDENTIALS="0"
          export METADATA_URL=""
          export GCE_METADATA_HOST=""
          unset METADATA_URL
          unset GCE_METADATA_HOST

          # Дополнительные переменные для отключения metadata service
          export NO_PROXY="*"
          export NO_METADATA="1"

          # Skip driver.ready() check in CI - it can hang in GitHub Actions
          # The driver will establish connection on first query instead
          export YDB_SKIP_READY_CHECK="true"
          echo "✅ Skipping driver.ready() check (YDB_SKIP_READY_CHECK=true) - connection will be established on first query"

          # Проверяем переменные окружения
          if [ -z "$YDB_ENDPOINT" ] || [ -z "$YDB_DATABASE" ]; then
            echo "❌ ERROR: YDB_ENDPOINT or YDB_DATABASE is not set"
            exit 1
          fi

          echo "🔌 Connecting to YDB..."
          echo "Endpoint: $YDB_ENDPOINT"
          echo "Database: $YDB_DATABASE"
          echo "Service Account Key File: $YC_SERVICE_ACCOUNT_KEY_FILE"
          echo ""
          echo "📋 Database path format check:"
          if [[ "$YDB_DATABASE" =~ ^/ru-central1/ ]]; then
            echo "  ✅ Database path format is correct (starts with /ru-central1/)"
            # Extract folder ID and database ID for verification
            if [[ "$YDB_DATABASE" =~ ^/ru-central1/([^/]+)/(.+)$ ]]; then
              FOLDER_ID="${BASH_REMATCH[1]}"
              DB_ID="${BASH_REMATCH[2]}"
              echo "  📁 Folder ID: $FOLDER_ID"
              echo "  🗄️  Database ID: $DB_ID"
            fi
          else
            echo "  ⚠️  WARNING: Database path should start with /ru-central1/"
            echo "  Current path: $YDB_DATABASE"
          fi
          echo ""
          echo "💡 If you see 'Database not found' error:"
          echo "  1. Check if database exists in Yandex Cloud Console:"
          echo "     https://console.cloud.yandex.ru/folders/$FOLDER_ID/ydb"
          echo "  2. Verify YDB_DATABASE secret matches the database path in console"
          echo "     Expected format: /ru-central1/<cloud-id>/<database-id>"
          echo "     Note: Path uses Cloud ID (not Folder ID)"
          echo "  3. Ensure Service Account has 'YDB Editor' role on the database"
          echo "  4. Check Service Account key is valid and not expired"
          echo "  5. Verify YDB_ENDPOINT is: grpcs://ydb.serverless.yandexcloud.net:2135"
          echo "     (without ?database= parameter)"

          # Проверяем, что файл создан
          if [ ! -f "$YC_SERVICE_ACCOUNT_KEY_FILE" ]; then
            echo "❌ ERROR: Service Account key file not found"
            exit 1
          fi

          echo "✅ Service Account key file exists"

          # Извлекаем Service Account ID из ключа
          if command -v jq &> /dev/null; then
            SERVICE_ACCOUNT_ID=$(jq -r '.service_account_id' "$YC_SERVICE_ACCOUNT_KEY_FILE" 2>/dev/null || echo "unknown")
            echo "   Service Account ID: $SERVICE_ACCOUNT_ID"
          fi

          # Выводим полную информацию для диагностики
          echo ""
          echo "📊 Connection Details:"
          echo "  Endpoint: $YDB_ENDPOINT"
          echo "  Database Path: $YDB_DATABASE"
          if [[ "$YDB_ENDPOINT" == *"?database="* ]]; then
            echo "  ⚠️  WARNING: Endpoint already contains ?database= parameter"
            echo "  Full endpoint: $YDB_ENDPOINT"
            echo "  This might cause issues if YDB_DATABASE is also set"
          else
            # Формируем connectionString как в коде
            if [[ "$YDB_ENDPOINT" == */ ]]; then
              SEPARATOR="?"
            else
              SEPARATOR="/?"
            fi
            EXPECTED_CS="${YDB_ENDPOINT}${SEPARATOR}database=$(echo "$YDB_DATABASE" | jq -sRr @uri 2>/dev/null || echo "$YDB_DATABASE")"
            echo "  Expected connectionString: ${EXPECTED_CS}"
          fi
          echo ""
          echo "💡 If you see 'Database not found' error:"
          echo "   1. Verify database exists: https://console.cloud.yandex.ru/folders/$FOLDER_ID/ydb"
          echo "   2. Check database path in console matches: $YDB_DATABASE"
          echo "      Expected format: /ru-central1/<cloud-id>/<database-id>"
          echo "      Note: Path uses Cloud ID (not Folder ID)"
          echo "   3. Ensure Service Account '$SERVICE_ACCOUNT_ID' has 'YDB Editor' role"
          echo "   4. Wait 1-2 minutes after assigning roles (they apply with delay)"
          echo "   5. Verify YDB_ENDPOINT is: grpcs://ydb.serverless.yandexcloud.net:2135"
          echo "      (without ?database= parameter)"
          echo ""

          echo ""
          echo "🚀 Running migrations..."
          npm run migrate

      - name: Check migration status
        timeout-minutes: 3
        env:
          YC_SERVICE_ACCOUNT_KEY: ${{ secrets.YC_SERVICE_ACCOUNT_KEY }}
          YDB_ENDPOINT: ${{ secrets.YDB_ENDPOINT }}
          YDB_DATABASE: ${{ secrets.YDB_DATABASE }}
        run: |
          cd backend

          # Создаём временный файл с Service Account ключом
          echo "$YC_SERVICE_ACCOUNT_KEY" > /tmp/yc-sa-key.json
          chmod 600 /tmp/yc-sa-key.json
          export YC_SERVICE_ACCOUNT_KEY_FILE="/tmp/yc-sa-key.json"
          # Оставляем YC_SERVICE_ACCOUNT_KEY для приоритета JSON строки над файлом
          export YC_SERVICE_ACCOUNT_KEY="$YC_SERVICE_ACCOUNT_KEY"

          # Явно отключаем metadata service (недоступен в GitHub Actions)
          export YDB_METADATA_CREDENTIALS="0"
          export METADATA_URL=""
          export GCE_METADATA_HOST=""
          export NO_METADATA="1"
          unset METADATA_URL
          unset GCE_METADATA_HOST

          # Skip driver.ready() check in CI - it can hang in GitHub Actions
          export YDB_SKIP_READY_CHECK="true"

          echo "📋 Checking migration status..."
          npm run migrate:status
