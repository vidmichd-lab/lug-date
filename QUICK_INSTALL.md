# ⚡ Быстрая установка инструментов

## ✅ Уже установлено

- ✅ **Yandex Cloud CLI** - установлен и готов к использованию
- ✅ **Git** - уже установлен в системе

## 🔧 Что нужно установить

Для полного деплоя нужно установить еще 3 инструмента:

### 1. Node.js и npm

**Способ 1: Через официальный установщик (рекомендуется)**
1. Откройте: https://nodejs.org/
2. Скачайте LTS версию (рекомендуется)
3. Установите .pkg файл
4. Перезапустите терминал

**Способ 2: Через Homebrew (если установлен)**
```bash
brew install node
```

**Проверка:**
```bash
node --version
npm --version
```

### 2. Docker Desktop

1. Откройте: https://www.docker.com/products/docker-desktop
2. Скачайте Docker Desktop для Mac
3. Установите .dmg файл
4. Запустите Docker Desktop из Applications
5. Дождитесь полной загрузки (иконка в меню перестанет мигать)

**Проверка:**
```bash
docker --version
docker ps
```

### 3. Homebrew (опционально, для удобства)

Если хотите устанавливать пакеты через Homebrew:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

После установки добавьте в PATH (для Apple Silicon):
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

---

## 🚀 После установки

1. **Перезапустите терминал** или выполните:
   ```bash
   source ~/.zprofile
   source ~/.zshrc
   ```

2. **Проверьте все инструменты:**
   ```bash
   node --version
   npm --version
   git --version
   docker --version
   yc version
   ```

3. **Настройте Yandex Cloud CLI:**
   ```bash
   yc init
   ```
   Следуйте инструкциям на экране.

4. **Запустите деплой:**
   ```bash
   cd /Users/timitro/Downloads/lug
   ./scripts/deploy-all.sh
   ```

---

## 📋 Альтернатива: GitHub Actions

Если установка инструментов вызывает проблемы, используйте автоматический деплой через GitHub Actions (не требует локальной установки):

```bash
cd /Users/timitro/Downloads/lug
git add .
git commit -m "Deploy to Yandex Cloud"
git push origin main
```

После push автоматически запустится деплой на Yandex Cloud.

Проверить статус: https://github.com/vidmichd-lab/lug-date/actions

---

## ❓ Помощь

Если возникли проблемы:
- См. [INSTALL_TOOLS.md](INSTALL_TOOLS.md) - подробная инструкция
- См. [DEPLOY_NOW.md](DEPLOY_NOW.md) - инструкция по деплою

