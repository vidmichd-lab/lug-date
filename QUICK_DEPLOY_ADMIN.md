# 🚀 Быстрый деплой админки

## Проблема с ключами

Ошибка `The request signature we calculated does not match` означает, что:

1. Бакет `lug-admin-deploy` не существует, ИЛИ
2. Ключи доступа неправильные, ИЛИ
3. Ключи не имеют прав на запись в бакет

## ✅ Быстрое решение

### Шаг 1: Создайте бакет

1. Откройте https://console.cloud.yandex.ru/
2. Object Storage → Buckets → **Создать bucket**
3. Имя: `lug-admin-deploy`
4. Доступ: **Публичный**
5. Регион: `ru-central1`
6. **Создать**

### Шаг 2: Создайте ключи доступа

1. Object Storage → **Service accounts**
2. Выберите сервисный аккаунт (или создайте)
3. **Ключи доступа** → **Создать статический ключ**
4. Скопируйте **Access Key ID** и **Secret Access Key**

### Шаг 3: Обновите .env

Добавьте в `.env`:

```env
ADMIN_STORAGE_BUCKET_DEV=lug-admin-deploy
ADMIN_STORAGE_ACCESS_KEY_DEV=ВАШ_ACCESS_KEY_ID
ADMIN_STORAGE_SECRET_KEY_DEV=ВАШ_SECRET_ACCESS_KEY
```

### Шаг 4: Задеплойте

```bash
npm run deploy:admin
```

### Шаг 5: Включите статический хостинг

1. Откройте бакет `lug-admin-deploy`
2. **Настройки** → **Статический сайт**
3. Включите "Статический сайт"
4. Главная страница: `index.html`
5. Страница ошибки: `index.html`

### Шаг 6: Откройте админку

URL: `https://lug-admin-deploy.website.yandexcloud.net/`

---

## 🔄 Альтернатива: Использовать существующий бакет

Если у вас уже есть бакет `dating-app-storage`:

1. **Создайте подпапку для админки:**
   - Откройте бакет
   - Создайте папку `admin/`

2. **Измените скрипт деплоя** чтобы загружать в подпапку, ИЛИ

3. **Используйте другой бакет:**

   ```env
   ADMIN_STORAGE_BUCKET_DEV=dating-app-storage
   ADMIN_STORAGE_ACCESS_KEY_DEV=<ваш_access_key_id>
   ADMIN_STORAGE_SECRET_KEY_DEV=<ваш_secret_access_key>
   ```

   **⚠️ ВАЖНО:** Получите реальные ключи в Yandex Cloud Console → IAM → Service Accounts → Access Keys

---

## ⚠️ Важно

- Бакет должен быть **публичным** для статического хостинга
- Ключи должны иметь права **storage.editor** или **storage.admin**
- После создания бакета подождите 1-2 минуты перед деплоем

---

## ✅ После успешного деплоя

Вы увидите:

```
✨ Deployment complete!
   ✅ Uploaded: 11 files

🌐 URLs:
   Storage: https://storage.yandexcloud.net/lug-admin-deploy/
   Website: https://lug-admin-deploy.website.yandexcloud.net/
```

Админка будет доступна по Website URL! 🎉
